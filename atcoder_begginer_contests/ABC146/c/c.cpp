#include <bits/stdc++.h>
using namespace std;

long long INF = pow(10, 19);

int main() {
  long long a, b, x;
  cin >> a >> b >> x;

  long long right = INF;
  long long left = 0;
  long long mid = (INF + 0) / 2.0;

  return 0;
}