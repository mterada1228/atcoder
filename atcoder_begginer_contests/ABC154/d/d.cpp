#include <bits/stdc++.h>
using namespace std;

int main() {
  int N, K;

  cin >> N >> K;

  vector<double> S(N + 1, 0);

  for (int i = 0; i < N; ++i) {
    double p;
    cin >> p;
  }

  return 0;
}